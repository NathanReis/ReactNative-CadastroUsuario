import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    alignItems: 'center',
    justifyContent: 'center',

    width: 160,

    padding: 8,
    margin: 8,

    borderColor: 'white',
    borderWidth: 1,
    borderRadius: 16
  },
  title: {
    color: 'white',
    fontWeight: 'bold'
  }
});
